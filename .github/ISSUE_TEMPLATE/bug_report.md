---
name: Bug report
about: Report unexpected behavior in `semantic-toponav`
title: "[BUG] "
labels: bug
assignees: ''
---

## What happened

<!-- A clear, concise description of what went wrong. -->

## What you expected

<!-- What the API/CLI should have done instead. -->

## Reproducer

<!--
Minimal snippet or CLI invocation that triggers the bug.
Ideally one that runs against the bundled `examples/` so we can run it
verbatim. If your reproducer needs a custom graph, paste the YAML inline.
-->

```python
# or:
# semantic-toponav plan examples/indoor_office.yaml ... 
```

## Actual output

```
<!-- error message, traceback, or wrong-but-not-erroring output -->
```

## Environment

- `semantic-toponav` version (or commit SHA):
- Python version: `python --version`
- OS:
- Installed extras (`viz`, `viz_web`, `map`):
- ROS2 distro (if relevant for the `ros2/` adapter or rosbag loader):
